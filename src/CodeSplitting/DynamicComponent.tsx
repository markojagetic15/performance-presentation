const DynamicComponent = () => (
    <div>
        <p>This is a dynamically loaded component!</p>
        <p>This is a dynamically loaded component!</p>
        <p>This is a dynamically loaded component!</p>
        <p>This is a dynamically loaded component!</p>
        <p>This is a dynamically loaded component!</p>
        <p>This is a dynamically loaded component!</p>
    </div>
);

export default DynamicComponent;